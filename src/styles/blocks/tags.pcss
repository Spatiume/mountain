.tags {
  display: flex;
  flex-wrap: wrap;
}

.tags__item {
  margin-right: 15px;
  margin-bottom: 15px;
}

.tag {
  padding: 13px 21px;
  background: #dde8f4;
  font-size: 15px;
  font-weight: 600;
  border-radius: 50px;
  opacity: 0.8;
}

